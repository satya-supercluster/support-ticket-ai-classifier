apiVersion: v1
kind: Service
metadata:
  name: ticket-classifier-service
  namespace: production
  labels:
    app: ticket-classifier
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ticket-classifier